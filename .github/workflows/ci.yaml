name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --frozen

      - name: Lint with ruff
        run: uv run ruff check . && uv run ruff format --check .

      - name: Type checking with mypy
        run: uv run mypy . || true

      - name: Run tests
        run: |
          if [ -d "backend/tests" ]; then
            rm -f test.db *.db
            DATABASE_URL=sqlite+aiosqlite:///./test.db uv run pytest backend/tests/ -v
          fi

      - name: Summary
        run: echo "✅ Build successful!"
